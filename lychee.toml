# Link checker config.
exclude = [
  # Placeholder profile links until CJ fills in real handles.
  "USERNAME",
  # LinkedIn blocks bots with 999s.
  "linkedin\\.com",
  # Site's own production URL — 404s until the first Pages deploy succeeds,
  # so checking it here is a chicken-and-egg failure on the very first run.
  "cj-ting\\.github\\.io/cj_portfolio",
]
no_progress = true
max_retries = 2
timeout = 20
